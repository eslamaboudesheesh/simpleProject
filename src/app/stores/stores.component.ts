import { Component, OnDestroy } from '@angular/core';
import { ChangeDetectorRef } from '@angular/core';

@Component({
    selector: 'stores-pages',
    template: ``,
})
export class StoresComponent {

    constructor() { }
}
